MONGO_URI=mongodb://localhost:27017/universe
JWT_SECRET=replace_with_a_long_random_string
PORT=5000

# Optional - only needed for /api/ai/chat. Server runs fine without it,
# that route will just return 503 until this is set.
GROQ_API_KEY=